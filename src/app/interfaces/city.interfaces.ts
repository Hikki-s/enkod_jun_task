export interface ICity {
  id: string;
  image: string;
  name: string;
  description: string;
  favorite: boolean;
}

export interface ICityPost {
  image: string;
  name: string;
  description: string;
  favorite: boolean;
}
